services:
  chat-backend:
    container_name: chat-backend
    build: 
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 5000:5000
    env_file:
      - .env